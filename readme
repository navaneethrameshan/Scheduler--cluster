Project: 
      Cloud Computing
      Load-balancer/Scheduler
      
To run: 
      Type 'make'
      Run with example config './cc'
      A log file called 'cloud.log' will be stored in the same directory

To generate graphs:
      After a run, type 'make graphs'
      Inside the 'graphs/' folder, the files of interest are *.png

Cleanup:
      Type 'make clean'

Known issues: 
      - Instructions per unit of time not correct, thus cost value is weird
      - Single or web mode isn't handled as it is scheduled using round-robin

Dependencies:
      - Python and gnuplot for generating graphs

Authors: 
      Navaneeth Rameshan
      Marcus Ljungblad
      Wasif Malik
