Project: 
      Cloud Computing
      Load-balancer/Scheduler
      
To run: 
      Type 'make'
      Run with example config './cc'
      A log file called 'cloud.log' will be stored in the same directory

Configuration files:
	modules/input.conf (used for generating tasks and jobs)
	modules/scheduler.conf (used for scheduler configuration)
	modules/workers.conf (used for initializing a set of workers)


To generate graphs:
 After a run, type 'make graphs'
      Inside the 'graphs/' folder, the files of interest are *.png

Cleanup:
      Type 'make clean'

Dependencies:
      - Python and gnuplot for generating graphs

Authors: 
      Navaneeth Rameshan
      Marcus Ljungblad
      Wasif Malik
